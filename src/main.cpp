#include "Menu.h"
#include <iostream>

// Main function to start the options menu
// Precondition: Menu class must be defined and have a startMenu method
// Postcondition: Initializes and starts the options menu, returns 0 upon successful execution
int main()
{
  Menu optionsMenu;

  optionsMenu.startMenu();

  return 0;
}